# CHANGES

- Preserved the original source file in `raw/大作业2.cpp`.
- Split the original single-file implementation into `include/` and `src/`.
- Kept the original data-structure choices: custom `VecList`, `LinkStack`, `LinkQueue`, adjacency matrix graph, Dijkstra, and Yen-style KSP logic.
- Added include guards through `#pragma once`.
- Added `const` qualifiers to several accessor methods so the split headers compile cleanly.
- Removed some debug printing from `main.cpp` to make sample output easier to read.
- Added README with project goal, data structures, algorithm flow, commands, sample input/output, complexity analysis, and course reflection.
